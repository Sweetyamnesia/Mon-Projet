void demarrer_session_jeu(void)
{
	//Sélectionner l'utilisateur actif
	//Choisir un jeu (pour l'instant 1 mini-jeu)
	//lancer le jeu
	//calculer un score
	//appeler save_session(score_utilisateur)
}

/**
 * Fonction : menu utilisateur connecté
    Afficher :
        1. Lancer une session VR
        2. Voir l'historique des sessions
        3. Se déconnecter

    Si utilisateur choisit 1 :
        Lancer une nouvelle session
            Simuler un exercice VR (ex: appuyer sur des touches pour représenter un mini-jeu)
            Chronométrer ou compter les réussites
            Sauvegarder les résultats (appeler history.c)

    Si utilisateur choisit 2 :
        Afficher historique (appeler history.c)

    Si utilisateur choisit 3 :
        Retourner au menu principal

 */

